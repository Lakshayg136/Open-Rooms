import 'package:flutter/material.dart';

class Pallete {
  static const Color backgroundColor = Color.fromRGBO(0, 0, 0, 1);
  static const Color primaryColor = Colors.white;
  static const Color secondaryColor = Colors.grey;
  static const Color transparent = Colors.transparent;

  static const Color gradient1 = Color.fromRGBO(0, 0, 0, 0);
  static const Color gradient2 = Color.fromRGBO(0, 0, 0, 0);
  static const Color gradient3 = Color.fromRGBO(0, 0, 0, 0);
  
  static const Color borderColor = Color.fromRGBO(0, 0, 0, 0);
}
